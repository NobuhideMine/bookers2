
<div class="container">
    <div class="row">
        <!--<div>frashireruWelcome! You have signed up successfully.サインアップ</div>-->
        <!--Signed out successfully.logout-->
        <!-- Signed in successfully.log in-->
        <%= render 'books/book', user: @user, book: @book %>
   
        <div class="col-md-8 offset-md-1">
            <h1>Books</h1>
            
            <table class="table table-hover">
                <tr>
                    <td></td>
                    <td>Title</td>
                    <td>Opinion</td>
                </tr>
                <% @books.each do |book| %>
                    <tr>
                        <td>
                            <%= link_to user_path do %>
                                <%= image_tag @user.get_profile_image(100,100) %>
                            <% end %>
                        </td>
                        <td><%= link_to book.title, book_path(book.id) %></td>
                        <td><%= book.body %> </td>
                    </tr>
                <% end %>
            </table>
            
            
            
             
        </div>
    
    </div>
</div>

